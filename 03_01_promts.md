What are some of the common headers that Apigee adds to an API call when a request goes through it?

# Create examples for the headers
Create a .csv file (called apigee_headers.csv) that has 3 columns:
* Header name
* Valid Header value
* Invalid Header value

For each of the headers identified above, create at least one example in the csv file.